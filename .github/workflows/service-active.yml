name: Keep Azure App Alive

   on:
     schedule:
       - cron: '*/45 * * * *'  # Every 45 minutes
     workflow_dispatch:  # Allows manual trigger

   jobs:
     ping:
       runs-on: ubuntu-latest
       steps:
         - name: Ping website
           run: curl -I https://gms-docs-ctc.azurewebsites.net/